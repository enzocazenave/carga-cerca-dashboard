# Puerto del servidor (Railway lo define automáticamente)
PORT=3000

# Ruta del archivo SQLite.
# Local:
DATABASE_PATH=./data/cargacerca.sqlite
# Railway con Volume montado en /data:
# DATABASE_PATH=/data/cargacerca.sqlite
